services:
  echa-mcp:
    build: .
    container_name: echa-mcp-server
    ports:
      - "8005:8005"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
